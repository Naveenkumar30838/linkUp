<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <style>
        .editPostForm{
            display: flex;
            flex-direction: column;
            row-gap: 20px; 
            width: fit-content;
            margin-top: 30px;
            background-color: rgb(179, 221, 237);
            padding: 12px;
            border-radius: 12px;
        }
        .editPostForm div{
            display: flex;
            justify-content: space-between;
            column-gap: 10px;

        }
    </style><link rel="stylesheet" href="/style.css">
</head>
<body>
    <!-- Header  -->
        <%- include("includes/header.ejs") %>

    <h2>Edit Post</h2>
    
    <p>Id : @<%=post.id %></p>
    <form action="/posts/<%=post.id%>?_method=PATCH" method="post" class="editPostForm">
        <div>
            <label for="username">username</label>
            <input type="username" name="username" value="<%=username%>" readonly>
        </div>
        <div>
            <label for="Password">Password</label>
            <input type="password" name="password" required>
        </div>
        <div>
            <label for="content">Content</label>
            <textarea name="content" id="" rows="3" cols="30"><%=post.content%></textarea>
        </div>
        <div>
            <label for="image">Image</label>
            <input type="url" name="Image" value=<%=post.Image%>>
        </div>
        <button type="submit">Confirm and Submit</button>
    </form>
</body>
</html>